import 'package:flutter/material.dart';

class AddToppingPage extends StatelessWidget {
  const AddToppingPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
